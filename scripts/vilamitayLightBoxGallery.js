$(document).ready(function () {
  $(".loft-gallery").magnificPopup({
    delegate: "a", // child items selector, by clicking on it popup will open
    type: "image",
    gallery: { enabled: true },
    // other options
  });
});

$(document).ready(function () {
  $(".galpao-gallery").magnificPopup({
    delegate: "a", // child items selector, by clicking on it popup will open
    type: "image",
    gallery: { enabled: true },
    // other options
  });
});

$(document).ready(function () {
  $(".casaantiga-gallery").magnificPopup({
    delegate: "a", // child items selector, by clicking on it popup will open
    type: "image",
    gallery: { enabled: true },
    // other options
  });
});

$(document).ready(function () {
  $(".commonarea-gallery").magnificPopup({
    delegate: "a", // child items selector, by clicking on it popup will open
    type: "image",
    gallery: { enabled: true },
    // other options
  });
});
